{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "lerna": "^8.1.9"
  },
  "scripts": {
    "build": "npx lerna run build",
    "prepare": "husky"
  }
}
